# 🔍 Чеклист проверки перед production

## 📋 Пред-запуск

- [ ] Docker установлен (версия 20.10+)
- [ ] Docker Compose установлен (версия 2.0+)
- [ ] Git установлен
- [ ] 8GB+ RAM доступно
- [ ] 20GB+ свободного места на диске

## 🔧 Конфигурация

- [ ] `backend/.env` создан из `.env.example`
- [ ] `frontend/.env` создан из `.env.example`
- [ ] `SECRET_KEY` в backend/.env изменён на длинный ключ
- [ ] Все пароли в .env.example НЕ используются в production
- [ ] `RTMP_SERVER_URL` правильно указан
- [ ] `CORS_ORIGINS` правильно настроены
- [ ] DATABASE_URL правильно указан

## 🐳 Docker

- [ ] `docker-compose ps` показывает все 9 сервисов
- [ ] Все сервисы имеют статус "Up" или "healthy"
- [ ] Нет ошибок в `docker-compose logs`
- [ ] PostgreSQL инициализирован успешно
- [ ] Redis доступен
- [ ] RTMP сервер запущен

## 🌐 Сетевые порты

- [ ] Port 80 (HTTP) - NGINX
- [ ] Port 443 (HTTPS) - NGINX (если SSL включен)
- [ ] Port 3000 (React Dev) - Frontend
- [ ] Port 5432 (PostgreSQL) - База данных
- [ ] Port 6379 (Redis) - Кэш
- [ ] Port 8000 (FastAPI) - API
- [ ] Port 8080 (RTMP Stats) - RTMP Stats
- [ ] Port 1935 (RTMP) - RTMP Server
- [ ] Port 9090 (Prometheus) - Метрики
- [ ] Port 3000 (Grafana) - Графики

## 🔗 Проверка HTTP endpoints

Запуск curl команд:

```bash
# Главное приложение
curl http://localhost/

# API документация
curl http://localhost:8000/docs

# API health check
curl http://localhost:8000/health

# RTMP stats
curl http://localhost:8080/stat

# Prometheus
curl http://localhost:9090

# Grafana
curl http://localhost:3000
```

- [ ] Главное приложение (http://localhost) загружается
- [ ] API docs (http://localhost:8000/docs) доступны
- [ ] API health check возвращает 200
- [ ] RTMP stats доступны
- [ ] Prometheus доступен
- [ ] Grafana доступна

## 🗄️ База данных

```bash
# Проверить таблицы
docker-compose exec postgres psql -U postgres -d xatube -c "\dt"
```

- [ ] Таблица `users` существует
- [ ] Таблица `channels` существует
- [ ] Таблица `streams` существует
- [ ] Таблица `stream_views` существует
- [ ] Таблица `statistics` существует
- [ ] Таблица `documents` существует
- [ ] В таблице `documents` есть 3 документа

## 👤 Аутентификация

1. Открыть http://localhost
2. Нажать "Регистрация"
3. Заполнить форму:
   - Username: `testuser`
   - Email: `test@example.com`
   - Password: `Test123!@#`
   - Full Name: `Test User`

- [ ] Регистрация прошла успешно
- [ ] Автоматический вход произошёл
- [ ] Пользователь в БД (проверить: `SELECT * FROM users;`)

## 🎥 RTMP и OBS

1. В профиле скопировать RTMP URL и ключ
2. Открыть OBS Studio
3. Settings → Stream:
   - Service: `Custom`
   - Server: `rtmp://localhost:1935/live`
   - Stream Key: Вставить ключ из профиля
4. Нажать "Start Streaming"

- [ ] OBS подключается к серверу
- [ ] OBS статус показывает "Connected"
- [ ] На главной странице трансляция отображается
- [ ] Live бейдж видно на карточке
- [ ] Плеер показывает live индикатор

## 📊 Фронтэнд функции

### Главная страница
- [ ] Загружается без ошибок
- [ ] Отображаются трансляции (если есть)
- [ ] Фильтры работают
- [ ] Поиск работает
- [ ] Карточки потоков отображаются правильно

### Профиль
- [ ] Форма редактирования профиля видна
- [ ] Вкладка "Ключ трансляции" работает
- [ ] RTMP URL отображается
- [ ] Кнопка "Копировать" работает
- [ ] OBS инструкции видны
- [ ] Кнопка регенерации ключа работает

### Статистика
- [ ] Карточки со статистикой отображаются
- [ ] Таблица дневной статистики видна
- [ ] Селектор периода работает
- [ ] Топ видео отображается
- [ ] Обновление данных работает

### Видеоплеер
- [ ] Плеер загружается
- [ ] Видео отображается
- [ ] Контролы работают
- [ ] Мут/Громкость работают
- [ ] Полноэкран работает

## 📈 Мониторинг

### Prometheus
- [ ] http://localhost:9090 доступен
- [ ] Targets статус "UP"
- [ ] Метрики отображаются

### Grafana
- [ ] http://localhost:3000 доступна
- [ ] Логин работает (admin/password)
- [ ] Дашборды загружаются
- [ ] Графики отображают данные

## 🔒 Безопасность

- [ ] Пароли не хранятся в plain text
- [ ] CORS настроен на конкретные origins
- [ ] JWT токены работают правильно
- [ ] Rate limiting работает
- [ ] Security headers присутствуют

```bash
# Проверить headers
curl -I http://localhost/
```

- [ ] X-Frame-Options присутствует
- [ ] X-Content-Type-Options присутствует
- [ ] Strict-Transport-Security присутствует

## 📝 Логирование

```bash
# Проверить логи
docker-compose logs -f backend | head -20
docker-compose logs -f frontend | head -20
docker-compose logs -f nginx | head -20
```

- [ ] Нет ERROR сообщений
- [ ] Нет WARNING сообщений
- [ ] API запросы логируются

## 🚀 Performance

```bash
# Проверить загрузку CPU/Memory
docker stats
```

- [ ] Backend использует < 500MB
- [ ] Frontend использует < 300MB
- [ ] PostgreSQL использует < 500MB
- [ ] CPU использование < 50%

## 📱 Responsive дизайн

### Desktop (1920x1080)
- [ ] Все компоненты видны
- [ ] Макет оптимален
- [ ] Нет горизонтального скролла

### Tablet (768x1024)
- [ ] Меню адаптивно
- [ ] Сетка потоков в 2 колонки
- [ ] Читабельно

### Mobile (375x667)
- [ ] Меню скрыто в бургер
- [ ] Сетка потоков в 1 колонку
- [ ] Размер шрифта подходит
- [ ] Кнопки кликабельны

## ✅ Финальная проверка

Контрольный лист для production:

```bash
# 1. Все сервисы запущены?
docker-compose ps | grep "Up"

# 2. Нет ошибок в логах?
docker-compose logs --tail=50 | grep ERROR

# 3. БД здорова?
docker-compose exec postgres pg_isready

# 4. Redis здоров?
docker-compose exec cache redis-cli ping

# 5. API отвечает?
curl -s http://localhost:8000/docs | grep -q "Swagger"

# 6. Фронтэнд загружается?
curl -s http://localhost/ | grep -q "XaTube"
```

- [ ] Все сервисы Up
- [ ] Нет ERROR логов
- [ ] PostgreSQL готов
- [ ] Redis готов
- [ ] API работает
- [ ] Фронтэнд работает

---

## 📞 Если что-то не работает

### Backend не запускается
```bash
docker-compose logs backend
# Проверить requirements.txt
# Проверить Python версию
```

### Frontend не загружается
```bash
docker-compose logs frontend
# Проверить package.json
# Очистить node_modules
```

### БД не инициализируется
```bash
docker-compose down -v
docker volume prune -f
docker-compose up -d postgres
sleep 30
docker-compose up -d
```

### RTMP не работает
```bash
docker-compose logs rtmp
curl http://localhost:8080/stat
```

### Пустые страницы
```bash
# Очистить browser cache
# Проверить CORS origins
# Проверить .env файлы
```

---

## 🎉 Готово для production!

Если все галочки установлены, проект готов к:
- ✅ Развертыванию на сервере
- ✅ Запуску в production
- ✅ Использованию реальными пользователями
- ✅ Масштабированию

---

**Дата проверки**: _____________
**Проверяющий**: _____________
**Статус**: [ ] READY [ ] NOT READY

---

Удачи! 🚀
